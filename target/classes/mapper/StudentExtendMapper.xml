<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lee.mapper.StudentExtendMapper">

    <select id="getAllStudent" resultMap="com.lee.mapper.StudentMapper.BaseResultMap">
      SELECT * FROM Student;
    </select>

    <select id="selectNowIds" resultType="int">
        SELECT id FROM Student;
    </select>
</mapper>